import { MatchPassDirective } from './match-pass.directive';

describe('MatchPassDirective', () => {
  it('should create an instance', () => {
    const directive = new MatchPassDirective();
    expect(directive).toBeTruthy();
  });
});
